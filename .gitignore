# Build output. index.html is the deployed artifact and is committed; data.json
# is the same payload again, regenerated by `node build.mjs` in well under a second.
data.json

# Scratch API dumps from exploring the endpoint; superseded by cache/.
courses.json
courses-ferientyp3.json
courses_ferientyp3.json
